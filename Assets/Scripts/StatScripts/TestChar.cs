using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestChar : BaseChar
{
    // Start is called before the first frame update
    void Start()
    {
        allied = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

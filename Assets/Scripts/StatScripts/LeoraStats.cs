using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeoraStats : BaseStats
{

    // Start is called before the first frame update
    void Start()
    {
        charName = "Leora";
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}

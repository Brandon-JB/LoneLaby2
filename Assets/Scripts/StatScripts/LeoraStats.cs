using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Leora : BaseStats
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Leora";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
